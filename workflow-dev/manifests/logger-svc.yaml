apiVersion: v1
kind: Service
metadata:
  name: logger
  namespace: deis
  labels:
    heritage: deis
    app: logger
spec:
  ports:
  - port: 80
    name: http
    targetPort: http
  - port: 514
    name: transport
    targetPort: 1514
    protocol: UDP
  selector:
    app: logger
    